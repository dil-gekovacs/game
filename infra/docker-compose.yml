version: "3.9"

services:
  game:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ADDR=:8080
      - STATIC_DIR=/app/static
      - MAP_DIR=/app/assets/maps
    restart: unless-stopped
